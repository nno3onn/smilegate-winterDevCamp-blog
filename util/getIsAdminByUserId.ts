// import isAdmin from "./api/isAdmin";

// const getIsAdminByUserId = async (user_id: number) => {
//   const res = await isAdmin(user_id);
//   return res;
// };

// export default getIsAdminByUserId;
